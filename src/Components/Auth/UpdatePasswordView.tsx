import React from "react";
import { SignViewContainer, SignViewTitle } from "./SignViewShared";

const UpdatePasswordView = () => {
  return (
    <SignViewContainer>
      <SignViewTitle>Update Password</SignViewTitle>
    </SignViewContainer>
  );
};

export default UpdatePasswordView;
